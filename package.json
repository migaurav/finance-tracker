{
  "name": "finance-tracker",
  "version": "1.0.0",
  "description": "Personal Finance Tracker — React + Node + SQLite",
  "scripts": {
    "dev": "concurrently --names \"SERVER,CLIENT\" --prefix-colors \"cyan,magenta\" \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "start": "node --experimental-sqlite server/server.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
